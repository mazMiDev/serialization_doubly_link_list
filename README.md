# SerializationDoublyLinkList
## Сборка
```
 # Создайте папку для сборки и перейдите в неё
 mkdir build
 cd build

 # Конфигурация CMake
 cmake .. -G "Ninja"   # можно опустить -G, если хотите использовать генератор по умолчанию

 # Сборка
 cmake --build .
```